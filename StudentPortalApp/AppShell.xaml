<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StudentPortalApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:StudentPortalApp"
    xmlns:views="clr-namespace:StudentPortalApp.Views"
    Title="StudentPortalApp">

    <TabBar>
        <Tab Title="Home">
            <ShellContent
                Title="Home"
                ContentTemplate="{DataTemplate local:MainPage}"
                Route="MainPage" />
            
        </Tab>
        <Tab Title="Blue page">
            <ShellContent
                Title="Blue page"
                ContentTemplate="{DataTemplate views:NewPage1}"
                Route="NewPage1" />

        </Tab>
    </TabBar>

</Shell>
